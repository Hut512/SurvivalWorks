package de.survivalworkers.core.client.engine.io.keys;

/**
 * this interface is for every Class that wants to use {@link KeyHandler} to handle inputs
 * @see KeyHandler
 */
public interface KeyListener {
}
